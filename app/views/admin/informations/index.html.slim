.container
  .row
    = render 'layouts/admin_left_menu'
    = render 'admin/informations/information_list'
    .col-2
    .col-10
      - if @errors.any?
        .alert
          - @errors.each do |message|
            li = message
      = form_with model: Information, url: admin_informations_path, method: :post, local: true do |info|
        = render partial: 'form', locals: { f: info, edit: false }

    / = render 'admin/informations/information_list'
