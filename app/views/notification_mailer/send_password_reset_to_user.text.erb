<%= @user.name_sei + @user.name_mei %> 様

パスワードの再登録のページは以下になります。
<%= link_to 'こちらのリンクから変更してください', password_reset_url(user_id: @user.id, e_mail: @user.e_mail, token: true, only_path: false) %>

メールに身に覚えがない場合は削除してください。
